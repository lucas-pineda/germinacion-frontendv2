# Backend API base URL (autogerminador service)
# Used by the frontend to make HTTP requests and to derive the MQTT WebSocket URL.
# For local development, the backend defaults to http://localhost:8080.
VITE_API_URL=https://autogerminador-production.up.railway.app:8080
